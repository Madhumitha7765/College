# QUESTION -1 : Manipulate directory structures

# (a) Display the absolute path of your home directory
echo ~
pwd
echo $HOME

# (b) Create a new subdirectory called OS in your home directory.
mkdir OS

# (c) Create a new subdirectory called assign1 in OS.
mkdir OS/assign1

# (d) Create a new subdirectory called assign2 in OS
mkdir OS/assign2

# (e) Display the contents of the directory OS
ls OS

# (f) Delete the directory assign2.
rmdir OS/assign2

# (g) Display the contents of the directory OS
ls OS